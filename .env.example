# Copy this file to .env and fill in your OpenAI key. Never commit .env.
OPENAI_API_KEY=

# Passwords for the three database roles. Change them if the database is reachable by anyone else.
POSTGRES_PASSWORD=postgres-demo-password
APP_DB_PASSWORD=app-demo-password
AGENT_DB_PASSWORD=agent-demo-password
